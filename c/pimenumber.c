#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool isPrime(int n);

void main()
{
    int n = 3;
    printf("hello world !!");

    bool pime = isPrime(n);
    printf("%d \n", pime);
}

bool isPrime(int n)
{
    return true;
}